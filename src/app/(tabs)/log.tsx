import React from 'react';
import { View, Text } from 'react-native';

export default function LogScreen() {
  return (
    <View className="flex-1 bg-[#0d1117] items-center justify-center">
      <Text className="text-gold">شاشة السجل (قريباً)</Text>
    </View>
  );
}
